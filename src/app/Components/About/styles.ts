"use client";
import React from "react";
import styled from "@mui/system/styled";
import { Typography } from "@mui/material";

export const AboutCard = styled(Typography)(() => ({
  position: "relative",
  width: "100% !important",
  height: "100% !important",
  ObjectFit: "contain",
}));
